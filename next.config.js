/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: 
  {
    domains: 
    [
      "rb.gy",
      "drive.google.com",
    ],
  },

  
}

module.exports = nextConfig
